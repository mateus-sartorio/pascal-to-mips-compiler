# Origin of the compiler this app proxies. Read at build time by next.config.mjs, so a
# change to it on Vercel needs a redeploy, not just a restart. The EC2 public IP changes
# whenever the instance is stopped and started — an Elastic IP avoids that.
UPSTREAM_ORIGIN=http://3.134.82.40:8080
